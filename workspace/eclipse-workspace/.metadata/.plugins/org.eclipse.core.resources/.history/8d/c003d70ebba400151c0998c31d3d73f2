#include "includes.h"
class Examples {

public:

	int add() {
		int x = random() + 10;
		int y = random() + 10;
		printf("%s %d %s %d", "x: " + x + "\ny:" + y);
		return x + y;
	}

	int sub() {
		int x = random() + 10;
		int y = random() + 10;
		printf("x: " + x + "\ny: " + y);
		return x - y;
	}
};
